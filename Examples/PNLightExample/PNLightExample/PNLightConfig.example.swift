import Foundation

// Template config. Copy this file to `PNLightConfig.swift` and fill in your
// values. `PNLightConfig.swift` is gitignored so your API key is never committed.
enum PNLightConfig {
    /// Replace with your actual API key.
    static let apiKey: String = "YOUR_API_KEY"

    /// Placement name for the paywall.
    static let paywallPlacement: String = "paywall"
}